/**
 * @fileoverview Unit tests for js/constants/feature-flags.js
 * @generated by tests/setup/generate-module-tests.mjs
 */

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import {
  assertHasExports,
  assertModuleLoads,
} from '../../setup/module-test-utils.js';

const MODULE = '../../../js/constants/feature-flags.js';

describe('js/constants/feature-flags.js', () => {
  it('loads and exposes exports', async () => {
    const mod = await import(MODULE);
    assertHasExports(mod, ["CLOUDINARY_CONFIG","ENABLE_PHOTO_UPLOAD","STORAGE_PHOTO_PATH"]);
  });

});
