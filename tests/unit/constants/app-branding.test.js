/**
 * @fileoverview Unit tests for js/constants/app-branding.js
 * @generated by tests/setup/generate-module-tests.mjs
 */

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import {
  assertHasExports,
  assertModuleLoads,
} from '../../setup/module-test-utils.js';

const MODULE = '../../../js/constants/app-branding.js';

describe('js/constants/app-branding.js', () => {
  it('loads and exposes exports', async () => {
    const mod = await import(MODULE);
    assertHasExports(mod, ["ORG_NAME","ORG_SUBTITLE"]);
  });

});
