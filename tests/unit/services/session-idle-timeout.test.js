/**
 * @fileoverview Unit tests for js/services/session-idle-timeout.js
 * @generated by tests/setup/generate-module-tests.mjs
 */

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import {
  assertHasExports,
  assertModuleLoads,
} from '../../setup/module-test-utils.js';

const MODULE = '../../../js/services/session-idle-timeout.js';

describe('js/services/session-idle-timeout.js', () => {
  it('loads and exposes exports', async () => {
    const mod = await import(MODULE);
    assertHasExports(mod, ["clearSessionActivityRecord","isSessionIdleExpiredByStoredActivity","startSessionIdleMonitor","stopSessionIdleMonitor","touchSessionActivityRecordNow"]);
  });

});
