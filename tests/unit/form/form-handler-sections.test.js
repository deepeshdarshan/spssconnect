/**
 * @fileoverview Unit tests for js/form/form-handler-sections.js
 * @generated by tests/setup/generate-module-tests.mjs
 */

import '../../setup/browser-globals.js';
import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import {
  assertHasExports,
  assertModuleLoads,
} from '../../setup/module-test-utils.js';

const MODULE = '../../../js/form/form-handler-sections.js';

describe('js/form/form-handler-sections.js', () => {
  it('loads and exposes exports', async () => {
    const mod = await import(MODULE);
    assertHasExports(mod, ["addMemberBlock","addNonMemberBlock","bindDynamicSections"]);
  });

});
